apiVersion: v1
kind: Service
metadata:
  name: url-shortener
  labels: {app.kubernetes.io/name: url-shortener}
spec:
  selector: {app.kubernetes.io/name: url-shortener}
  ports:
    - {name: http, port: 8080, targetPort: http}
