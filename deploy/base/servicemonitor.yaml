apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata: {name: url-shortener}
spec:
  selector:
    matchLabels: {app.kubernetes.io/name: url-shortener}
  endpoints:
    - {port: http, path: /metrics, interval: 30s}
