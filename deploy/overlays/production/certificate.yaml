apiVersion: cert-manager.io/v1
kind: Certificate
metadata: {name: url-shortener-tls}
spec:
  secretName: url-shortener-tls
  issuerRef: {name: cloudflare-dns01, kind: ClusterIssuer}
  dnsNames: ["*.url.shinkeonkim.com"]
